# ── Frontend Environment Variables ────────────────────────────────────────────
# Backend API URL (default: http://localhost:8000 for local dev)
# In Docker Compose, this is set via the build arg in docker-compose.yml
VITE_API_URL=http://localhost:8000
