# ── Backend Environment Variables ─────────────────────────────────────────────

# Supabase (required)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# OpenAI (required — powers the ReAct agent)
OPENAI_API_KEY=sk-...

# Tavily Web Search (optional — agent works without it but can't search the web)
TAVILY_API_KEY=tvly-...

# Google Trends MCP Server URL
# Local dev:   http://localhost:8080/mcp/
# Docker:      http://google-trends-mcp:8080/mcp/  (set in docker-compose.yml)
MCP_SERVER_URL=http://localhost:8080/mcp/

# Frontend URL for CORS
# Local dev:   http://localhost:5173
# Docker:      http://localhost:3000  (set in docker-compose.yml)
FRONTEND_URL=http://localhost:5173
